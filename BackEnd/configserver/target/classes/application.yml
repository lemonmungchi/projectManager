server:
  port: 8888

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/your-repo/config-repo # Config Repository URL
          clone-on-start: true

