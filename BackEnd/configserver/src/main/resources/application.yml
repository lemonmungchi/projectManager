server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/your-username/private-repo.git
          username: ${GITHUB_USERNAME}
          password: ${GITHUB_TOKEN}

